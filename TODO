- in configure.ac
* Remove hard dependency on Tao.* - enable examples if it's found, disable if it's not

- in the pick-object example, Engine.Throb() needs to be implemented

- in the rotate-buttons example non-numeric values should be stricken
  and replaced with the previous value

- Janne's LWO loader code should be used to create a new example for
  loading lightwave models

- Speak with John Carmack about the potential of letting me use one of
id's .mdl or .md3 files in a model-loading example

- build an object pick example